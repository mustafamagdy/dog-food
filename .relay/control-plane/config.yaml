# .relay/control-plane/config.yaml — per-project relay defaults (relay-5840 layout).
#
# Supersedes the legacy .relay/control-plane/defaults.json. Fields are all optional;
# every key from the JSON layout works here unchanged. See SPEC §11.H
# for the schema. Moving from defaults.json? Run `relay config migrate`.
{}
